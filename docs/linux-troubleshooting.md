# 🐧 Linux CLI Troubleshooting Toolkit

| Tool | Purpose |
| :--- | :--- |
| **htop / top** | Real-time CPU and process monitoring. |
| **tcpdump** | CLI-based packet capture (often exported to Wireshark). |
| **journalctl -xe** | Investigating systemd service failures. |
| **ss -tulpn** | Verifying listening ports and active socket connections. |
| **df -h / du -sh** | Identifying disk exhaustion and large log files. |
